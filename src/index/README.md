# index

Embedding and indexing layer.

Planned responsibilities:
- call Vertex AI embeddings
- write vectors to Vertex AI Vector Search
- map `chunk_id` <-> `vector_id`
- support separate indexes for `naive` and `semantic`
